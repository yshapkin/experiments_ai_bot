name: CI

on:
  push:
    branches:
      - master
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    name: Validate Node.js project
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Check out repository
        uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0

      - name: Validate Bicep
        shell: bash
        run: |
          set -euo pipefail

          formatted_file="$(mktemp)"
          trap 'rm -f "$formatted_file"' EXIT

          for file in deployment/main.bicep deployment/main.bicepparam; do
            az bicep format --file "$file" --stdout > "$formatted_file"
            diff -u "$file" "$formatted_file"
          done

          az bicep lint --file deployment/main.bicep
          az bicep build --file deployment/main.bicep --stdout > /dev/null
          az bicep build-params --file deployment/main.bicepparam --stdout > /dev/null

      - name: Set up Node.js
        uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Type-check
        run: npm run typecheck

      - name: Run tests
        run: npm test

      - name: Build production output
        run: npm run build
